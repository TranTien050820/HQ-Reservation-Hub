# Base URL of the HQ-WebOffice-API. Leave empty in dev to use the Vite proxy
# (see vite.config.ts) and call the API via relative "/api/..." paths instead.
# In production, point this at the real API host, e.g. https://api.example.com
VITE_API_BASE_URL=https://localhost:7018
